/** 
 * 
 */
package model

import view.Fx3DView
import java.util.List

/** 
 * @author morzzan
 * 
 */
event StartEvent{
	var fx : Fx3DView
	new(fx : Fx3DView){
		this.fx=fx
	}
}
event EndEvent 
event RegisterEvent{
	var nb : int
	new(nb : int){
		this.nb = nb
	}
}
event PerceptEvent{
	var pos : Vector3D
	var onZone : boolean
	var around : List<Vector3D>
	var speed : Vector3D
	new(pos : Vector3D,speed : Vector3D, around : List<Vector3D> = <Vector3D> newArrayList(),onZone : boolean){
		this.pos = pos
		this.around = around
		this.onZone = onZone
		this.speed=speed
	}
}
event GoThatWay{
	var direction : Vector3D
	new(direction : Vector3D){
		this.direction = direction
	}
}