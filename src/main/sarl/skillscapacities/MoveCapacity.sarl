/** 
 * 
 */
package skillscapacities

import model.Vector3D
import model.PerceptEvent

/** 
 * @author morzzan
 * 
 */
capacity MoveCapacity {
	def move(direction : Vector3D)
	def moveRandomly
	def moveTo(position : Vector3D)
	def getCurrentPercept : PerceptEvent
	def addPercept(p : PerceptEvent)
}
